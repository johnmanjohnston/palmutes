/*
  ==============================================================================

    SharedData.h
    Created: 15 Mar 2024 4:51:11pm
    Author:  USER

  ==============================================================================
*/

#pragma once
//#include <vector>

    /*
class SharedData {
//public:
    //static std::vector<int> activeNoteNumbers;

    void addNote(int noteNumber) { activeNoteNumbers.push_back(noteNumber); }
    void removeNote(int noteNumber)
    {
        for (auto it = activeNoteNumbers.begin(); it != activeNoteNumbers.end(); ++it) {
            if (*it == noteNumber) {
                activeNoteNumbers.erase(it);
                break;
            }
        }
    }
};
    */
